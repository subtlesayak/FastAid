---
phase: 10
plan: 1
status: complete
---

# Plan 10-01: HTTPS Pilot Readiness

<objective>
Make FastAid configurable for a controlled HTTPS pilot, distinguish liveness from readiness, prevent server-secret packaging, and provide an end-to-end field verification guide.
</objective>

## Tasks

- [x] Add pure pilot readiness evaluation and a `503` readiness endpoint.
- [x] Add build-time Android backend URL injection with loopback-only cleartext policy.
- [x] Add safe environment and Android local-property templates.
- [x] Add source/APK server-secret and tracked-pilot-data verification.
- [x] Document single-process HTTPS deployment and staged field UAT.
- [x] Add readiness tests, run the full backend suite, build the Android APK, and run the artifact gate.

## Verification

- `npm run check`
- `npm test`
- `npm run verify:pilot`
- `android\\gradlew.bat :app:assembleDebug`
- Pilot-mode readiness returns `503` when HTTPS/provider/admin requirements are missing and `200` when configured.
