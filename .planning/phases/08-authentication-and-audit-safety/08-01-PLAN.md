---
phase: 8
plan: 1
status: complete
---

# Plan 08-01: Authentication And Audit Boundary

<objective>
Protect responder and administrator lifecycle operations with short-lived sessions, role and ownership checks, secure credential storage, and append-only audit records.
</objective>

## Tasks

- [x] Extend the persistent store with users, sessions, and append-only audit collections.
- [x] Implement salted `scrypt` credentials and opaque hashed bearer sessions.
- [x] Add signup, login, logout, and current-user endpoints.
- [x] Enforce responder ownership and administrator role checks on protected routes.
- [x] Add administrator responder-verification and audit-list endpoints.
- [x] Add unit and HTTP integration coverage for authentication, authorization, persistence safety, and audit records.
- [x] Document the protected API contract and bootstrap configuration.

## Verification

- `npm run check`
- `npm test`
- The integration test confirms unauthenticated mutations fail, a verified responder can update only its own profile, and administrators can inspect the resulting audit trail.
- The persisted data file contains neither raw passwords nor bearer tokens.
